# Configurations
remote_theme: pages-themes/minimal@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one

title:            SiteGen - minimal static site builder
description:        Minimal static site builder based on Mojolicious framework

show_downloads: "true"
